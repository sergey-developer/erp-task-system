apiVersion: v1
kind: Service
metadata:
  name: {{ include "itsm-frontend.fullname" . }}
  labels:
    {{- include "itsm-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "itsm-frontend.selectorLabels" . | nindent 4 }}
