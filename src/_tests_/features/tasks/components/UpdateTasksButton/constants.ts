import { UpdateTasksButtonProps } from 'modules/task/components/UpdateTasksButton/index'

export const props: UpdateTasksButtonProps = {
  disabled: false,
  onClick: jest.fn(),
  onAutoUpdate: jest.fn(),
}
