import { UpdateTasksButtonProps } from 'features/tasks/components/UpdateTasksButton/index'

export const props: UpdateTasksButtonProps = {
  disabled: false,
  onClick: jest.fn(),
  onAutoUpdate: jest.fn(),
}
