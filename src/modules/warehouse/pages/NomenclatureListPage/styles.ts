import { Menu } from 'antd'
import styled from 'styled-components'

export const GroupListMenuStyled = styled(Menu)`
  max-height: 650px;
  margin-top: 55px;
  overflow-y: auto;
  overflow-x: hidden;
`
