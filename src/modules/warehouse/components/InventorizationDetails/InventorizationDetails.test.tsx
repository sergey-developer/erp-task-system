import { fakeId } from '_tests_/utils'

import { InventorizationDetailsProps } from './index'

const props: InventorizationDetailsProps = {
  open: true,
  inventorizationId: fakeId(),
  onClose: jest.fn(),
}

describe('Карточка инвентаризации', () => {
  test.todo('Карточка инвентаризации')
})
