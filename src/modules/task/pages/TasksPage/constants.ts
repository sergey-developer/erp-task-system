import { CSSProperties } from 'react'

export const DEFAULT_PAGE_SIZE = 100

export const tableItemBoundaryStyles: Pick<CSSProperties, 'borderBottom'> = {
  borderBottom: '5px solid mediumvioletred',
}
