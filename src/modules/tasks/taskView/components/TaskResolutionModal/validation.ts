import { Rule } from 'rc-field-form/lib/interface'

export const TECH_RESOLUTION_RULES: Rule[] = [
  {
    required: true,
    message: 'Обязательное поле',
    whitespace: true,
  },
]

export const USER_RESOLUTION_RULES: Rule[] = [
  {
    required: true,
    message: 'Обязательное поле',
    whitespace: true,
  },
]
