/**
 Задержка необходимая только для тестов, чтобы при запросах "rtk-query", менялся флаг isLoading и т.п.
 Без задержки данный флаг не меняется.
 */

export const API_RESPONSE_DELAY = 100

export const FAKE_ID = 1
